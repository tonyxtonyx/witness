server { listen 80; root /usr/share/nginx/html; index index.html; location /api/ { proxy_pass http://backend:8080; proxy_set_header X-Correlation-ID $request_id; } location / { try_files $uri /index.html; } }

